import React, { useState } from 'react';
import scheduleJson from '../../data/schedule_response.json'
import Days from "../Days/Days"

const Schedule = () => {
    
    
    return (
        <>
            {/* {schedules.map((schedule, index) => <Days schedule={schedule} key={index}/>)}
           */}
        </>
    );
};

export default Schedule;